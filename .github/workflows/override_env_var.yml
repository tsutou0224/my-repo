name: Override environment variables
on: 
  push:
    paths:
      - ".github/workflows/override_env_var.yml"
env:
  EXAMPLE: Defined by workflow level
jobs:
  print:
    runs-on: ubuntu-latest
    steps:
      - run: echo "${EXAMPLE}"
      - env:
          EXAMPLE: Defined by step level
        run: echo "${EXAMPLE}"
